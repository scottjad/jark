* 0.1 RELEASE
** long and short arg format
** multimethods for pretty-printing
** cp remove
** ns load FILE (add base dir to cp)
** ns alias NS NICK
** ng start --port --classpath 
** swank start [dynamic port]
** vm stat FIXES
*** uptime in readable format 
*** obtain swank port
** validations
** clin cp cljr [lib]

* 0.2 RELEASE
** Type inference
